name: Auto Merge Markdown Pull Requests

on:
  pull_request:
    paths:
      - 'season-*.md'

jobs:
  auto_merge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl

      - name: Download and install GitHub CLI
        run: |
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo gpg --dearmor -o /usr/share/keyrings/githubcli-archive-keyring.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
          sudo apt update
          sudo apt install gh -y

      - name: Check for only markdown changes
        id: check_markdown
        run: |
          git fetch origin seasons-source
          git diff-tree --no-commit-id --name-only -r origin/seasons-source HEAD | grep -q '^season-.*\.md$'
          if [ $? -eq 0 ]; then
            echo "::set-output name=markdown_only::true"
          else
            echo "::set-output name=markdown_only::false"
          fi

      - name: Auto-approve and merge
        if: steps.check_markdown.outputs.markdown_only == 'true'
        run: |
          cat "$GITHUB_EVENT_PATH"
          PR_NUMBER=$(jq --raw-output .pull_request.number "$GITHUB_EVENT_PATH")
          echo "PR_NUMBER: $PR_NUMBER"
          gh pr merge $PR_NUMBER --auto --squash --delete-branch
          gh pr comment $PR_NUMBER --body "This pull request has been automatically approved and merged because it only contains changes to markdown files."
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}

    
      - name: Comment on pull request
        if: steps.check_markdown.outputs.markdown_only == 'true'
        run: |
          gh pr comment ${{ github.event.pull_request.number }} --body "This pull request has been automatically approved and merged because it only contains changes to markdown files."
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}




