name: Conditional Check

on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run conditional checks
        run: |
          if [ "true" == "true" ]; then
            if [ "true" == "true" ]; then
              echo "Non-Markdown files have changed. Failing the check."
              exit 1
            else
              echo "Only Markdown files have changed. Check passed."
            fi
          fi
